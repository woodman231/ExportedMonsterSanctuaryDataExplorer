/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    unoptimized: true,
  },
  basePath: '/ExportedMonsterSanctuaryDataExplorer'
}

module.exports = nextConfig
